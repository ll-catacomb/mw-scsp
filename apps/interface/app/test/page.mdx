# Hello

## Testing MDX with shadcn/ui

This page demonstrates using shadcn/ui components directly in MDX.

### Buttons

<div className="flex gap-2 my-4">
  <Button>Default</Button>
  <Button variant="secondary">Secondary</Button>
  <Button variant="outline">Outline</Button>
  <Button variant="destructive">Destructive</Button>
</div>

### Badges

<div className="flex gap-2 my-4">
  <Badge>Default</Badge>
  <Badge variant="secondary">Secondary</Badge>
  <Badge variant="outline">Outline</Badge>
  <Badge variant="destructive">Destructive</Badge>
</div>

### Alert

<Alert className="my-4">
  <AlertTitle>Heads up!</AlertTitle>
  <AlertDescription>
    You can use shadcn/ui components directly in your MDX files.
  </AlertDescription>
</Alert>

### Card

<Card className="my-4">
  <CardHeader>
    <CardTitle>Card Title</CardTitle>
    <CardDescription>Card description goes here.</CardDescription>
  </CardHeader>
  <CardContent>
    <p>This is the card content. You can put anything here.</p>
  </CardContent>
  <CardFooter>
    <Button>Action</Button>
  </CardFooter>
</Card>

## Regular Markdown

You can still use all regular markdown features:

- Bullet points
- **Bold text**
- *Italic text*
- `inline code`

```js
// Code blocks work too
const greeting = "Hello, MDX!";
console.log(greeting);
```
